<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link
		rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		<a href="{{path('home')}}">home</a>
		<a href="{{path('oils')}}">Huiles Végétales</a>
		<a href="{{path('mentions')}}">Mentions légales</a>


			<div>
				<a href="{{path('blog')}}">Blog</a>
				<a href="{{path('cart')}}">Panier</a>
				<a href="{{path('admin_oils_index')}}">admin huiles</a>
				<a href="{{path('admin_families_index')}}">admin familles</a>
				<a href="{{path('admin_articles_index')}}">admin articles</a>
				<a href="{{path('app_login')}}">Connexion</a>
			</div>
	


		{% block body %}{% endblock %}
	</body>
</html>
